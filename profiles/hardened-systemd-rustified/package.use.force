# Swap out ``su`` from util-linux with the less unsafe one from sudo-rs.
app-admin/sudo-rs su

# Swap out Curl's SSL backend for rustls.
# Broken on 32-bit ABI.
# net-misc/curl rustls curl_ssl_rustls
